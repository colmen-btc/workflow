name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Build & Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run full test suite
        run: echo "TODO: Add your full test command (unit + integration)"

      - name: Generate SBOM
        run: echo "TODO: Add SBOM generator (e.g., syft, cyclonedx-bom)"

      - name: Build artifact
        run: echo "TODO: Add your build command"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
